name: my_app
description: "تطبيق نظام الاشتراكات المتقدم"

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter

  # أيقونات iOS وواجهة Cupertino
  cupertino_icons: ^1.0.8
  firebase_core: ^3.15.2
  firebase_auth: ^5.7.0
  cloud_firestore: ^5.4.0
  google_sign_in: ^6.2.1
  # مكتبة للتعامل مع التواريخ واللغات
  intl: ^0.18.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  # قواعد lint لفحص جودة الكود
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true

  # لو عندك صور أو ملفات داخل مجلد assets
  # افتح التعليق وعدل المسارات حسب مجلداتك
  # assets:
  #   - assets/images/
  #   - assets/icons/

  # يمكنك إضافة الخطوط العربية هنا
  #fonts: 
    #- family: Cairo
      #fonts:
        #- asset: fonts/Cairo-Regular.ttf
        #- asset: fonts/Cairo-Bold.ttf
          #weight: 700
